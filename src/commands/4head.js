// random 4head jokes 
/*
bot.ebot.on("message", (msg) => {
    if (!msg.messageText) return;
    if (msg.messageText.startsWith("^4Head")){
        // a database and pulls out a random string from that database

    }
})*/

